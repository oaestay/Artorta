= simple_form_for @press do |f|
  .row.text-center
    .col-xs-10.col-xs-offset-1.col-sm-6.col-sm-offset-3
      = f.input :name, label: "Nombre"
      = f.input :image, as: :file, label: "Imágen", placeholder: "Subir imagen"
      = f.input :popularity, label: "Popularidad"
      .clearfix
      = f.submit "Enviar", class: 'btn-block form-button'
